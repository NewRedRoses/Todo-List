export default function content() {
  const container = document.querySelector(".content");
  const sampleText = document.createElement("div");
  //   sampleText.textContent = "Testing container";
  container.appendChild(sampleText);
}
